<html>
    <body>
        <div>
            <% allPosts.forEach(p => { %>
                <div><%= p.title %></div>
                <div><%= p.body %></div>
            <% }) %>
        </div>
    </body>
</html>